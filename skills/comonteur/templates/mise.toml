[tools]
ffmpeg = "latest"

# The comonteur CLI — uncomment once comonteur has published its first release. Until
# then the `comonteur` binary on PATH is used (see `mise run comonteur:setup` in the
# comonteur repo, which does `cargo install --path cli`).
# "github:davidB/comonteur" = "0.1.0"

# Blender is deliberately not pinned here — install 5.2 LTS yourself
# (https://www.blender.org/download/lts/); `mise run comonteur:doctor` checks the version.
# Tasks live in mise-tasks/comonteur/, so this file stays yours to edit.
